#ifndef LATTICE_COORDINATION_NUMBER_HPP
#define LATTICE_COORDINATION_NUMBER_HPP

#include <map>
#include <string>

// Structure holding coordination numbers for different lattice types
extern const std::map<std::string, int> coordinationNumber;

#endif